import React from 'react';
import ReactDOM from 'react-dom/client';
import Interface from './components/Interface.js';

const store = ReactDOM.createRoot(document.querySelector('#root'));
store.render(<Interface />);
